{
    "payload": {
        "type": "object",
        "properties": {
            "client_id": { "type": "string" },
            "card_id": { "type": "string" },
            "order_id": { "type": "string" },
            "amount": { "type": "number" },
            "delivery": { "type": "number" },
            "cvv": { "type": "string" }
        }
    },
    "required": [
        "client_id",
        "card_id",
        "order_id",
        "amount",
        "delivery",
        "cvv"
    ],
    "additionalProperties": false
}
