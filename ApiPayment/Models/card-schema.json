{
    "payload": {
        "type": "object",
        "properties": {
            "holder_name": { "type": "string" },
            "number": { "type": "string" },
            "expiration_date": { "type": "string" },
            "cvv": { "type": "string" },
            "customer_id": { "type": "string" },
            "client_has_gateway": { "type": "string" }
        }
    },
    "required": [
        "holder_name",
        "number",
        "expiration_date",
        "cvv",
        "customer_id",
        "client_has_gateway"
    ],
    "additionalProperties": false
}
